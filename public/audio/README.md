# Audio assets

The public repository intentionally omits real music clips.

To enable full playback in the demo, place licensed short clips under:

```text
public/audio/library/
```

The expected filenames are listed in `src/data/tracks.js`.
